var mkFhir = require('fhir.js');

/**
 * 
 * @param fhirResource - resource to match against elements in the FHIR server
 * @param fieldFilter - fields to ignore or evaluate...  need to decide on details
 * @param resourceFilter - resources to ignore or match.  need to decide on details.
 */
function findPossibleMatches(fhirResource, fieldFilter, resourceFilter){
	//is it a bundle?
	
	//if not what is the top object type?
	
	//query based on object type
	
	//assign score to each possible match based on the difference in the objects
	
	//return array of the possible matches.
	
	//for a bundle a dictionary of objects containing the matches for all elements
}